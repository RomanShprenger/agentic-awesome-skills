{
  "skill_name": "modellix-design",
  "evals": [
    {
      "id": 1,
      "prompt": "Generate an image of a red panda sitting on a mossy stone in a misty bamboo forest at dawn. I don't care which model you use — just make it look good and save the file locally when it's done.",
      "expected_output": "Uses default T2I model google/nano-banana-2-lite via modellix-cli model run --wait (or equivalent), then task download (or persists the result locally). Does not invent a different default slug unless the user named one.",
      "files": [],
      "assertions": [
        "Used model slug google/nano-banana-2-lite (skill default for unspecified T2I)",
        "Submitted via modellix-cli model run (not inventing a non-CLI-only path when CLI is available)",
        "Used --wait or task wait instead of a hand-rolled task get polling loop",
        "Saved at least one image file locally under the run outputs directory",
        "Did not print or log the raw API key value"
      ]
    },
    {
      "id": 2,
      "prompt": "Make a short text-to-video of ocean waves under a cloudy sunset. No model preference from me — pick whatever the skill says is default, wait for it to finish, and download the mp4.",
      "expected_output": "Uses default T2V model bytedance/seedance-2.0-mini-t2v with model run --wait (longer timeout ok) and task download. Does not hand-roll a task get polling loop when CLI wait is available.",
      "files": [],
      "assertions": [
        "Used model slug bytedance/seedance-2.0-mini-t2v (skill default for unspecified T2V)",
        "Submitted via modellix-cli model run with wait (or model run + task wait)",
        "Did not implement a manual exponential-backoff task get poll loop while CLI wait is available",
        "Saved at least one .mp4 (or video) file locally under the run outputs directory",
        "Did not print or log the raw API key value"
      ]
    },
    {
      "id": 3,
      "prompt": "I want to use Modellix but I'm not sure if my CLI/auth is set up. Check my environment first, then if things look good generate a simple square image that says 'hello modellix' as typography art using the default image model.",
      "expected_output": "Runs modellix-cli doctor (or preflight wrapping doctor) before generation; proceeds with google/nano-banana-2-lite only after auth looks usable, or clearly asks for MODELLIX_API_KEY / auth login if missing.",
      "files": [],
      "assertions": [
        "Ran modellix-cli doctor or scripts/preflight.py before submitting a generation task",
        "Used model slug google/nano-banana-2-lite after environment check passed",
        "Produced a local image file under the run outputs directory",
        "Did not skip auth/environment check and jump straight to model run",
        "Did not print or log the raw API key value"
      ]
    },
    {
      "id": 4,
      "prompt": "Synthesize speech that says 'Welcome to Modellix' — I don't care which TTS model you use. Use the skill default, wait for completion, and save the audio locally.",
      "expected_output": "Uses default TTS model alibaba/qwen-audio-3.0-tts-flash with text + a Flash-compatible voice (e.g. longanhuan_v3.6) via model run --wait, then task download. Does not invent a different default slug unless the user named one.",
      "files": [],
      "assertions": [
        "Used model slug alibaba/qwen-audio-3.0-tts-flash (skill default for unspecified TTS)",
        "Request body included required text and voice fields",
        "Submitted via modellix-cli model run with wait (or model run + task wait)",
        "Saved at least one audio file locally under the run outputs directory",
        "Did not print or log the raw API key value"
      ]
    },
    {
      "id": 5,
      "prompt": "Transcribe this public audio URL with whatever STT model the skill recommends by default: https://example.com/meeting.mp3 — wait and show me the result (download if needed).",
      "expected_output": "Uses default STT model openai/whisper-1 with body url set to the given audio URL via model run --wait (and download/persist of the transcript resource when applicable).",
      "files": [],
      "assertions": [
        "Used model slug openai/whisper-1 (skill default for unspecified STT)",
        "Request body used the provided public audio url (not a file upload path)",
        "Submitted via modellix-cli model run with wait (or model run + task wait)",
        "Did not invent a different default STT slug unless the user named one",
        "Did not print or log the raw API key value"
      ]
    },
    {
      "id": 6,
      "prompt": "Clone the speaker from https://example.com/reference.wav and say 'There is a large garden behind my house.' — pick the skill's default speech-to-speech model and save the audio.",
      "expected_output": "Uses default STS model alibaba/cosyvoice-clone with required model (cosyvoice-v3.5-plus or cosyvoice-v3.5-flash), url, and text via model run --wait, then task download.",
      "files": [],
      "assertions": [
        "Used model slug alibaba/cosyvoice-clone (skill default for unspecified STS)",
        "Request body included model, url, and text",
        "Submitted via modellix-cli model run with wait (or model run + task wait)",
        "Saved at least one audio file locally under the run outputs directory",
        "Did not print or log the raw API key value"
      ]
    },
    {
      "id": 7,
      "prompt": "Generate an image of a cute cat with bytedance/seedream-5.0-pro. Look up that model's request schema first so the body is valid, then wait for the result and save the file locally.",
      "expected_output": "Calls modellix-cli model get-schema bytedance/seedream-5.0-pro before model run, uses the user-named slug (not the skill T2I default), builds --body from the schema's required fields, then model run --wait and task download.",
      "files": [],
      "assertions": [
        "Called modellix-cli model get-schema for bytedance/seedream-5.0-pro before submitting model run",
        "Used model slug bytedance/seedream-5.0-pro (user-named, not google/nano-banana-2-lite)",
        "Request body included required fields from the get-schema contract",
        "Submitted via modellix-cli model run with wait (or model run + task wait)",
        "Did not print or log the raw API key value"
      ]
    }
  ]
}
